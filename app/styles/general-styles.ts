import { StyleSheet } from 'react-native'

const generalStyles = StyleSheet.create({
  flex: {
    flex: 1
  },
  deviceSpacedContainer: {
    flex: 1,
    paddingHorizontal: 20
  }
})

export default generalStyles
